# Clinical Admin

Internal veterinary clinic administration system.

Security baseline: shared server-validated login, HttpOnly same-origin session cookie, private Supabase Storage for client files, least-privilege same-origin API proxies, RLS-deny-by-default database access, and a CSP that blocks inline JavaScript execution.
